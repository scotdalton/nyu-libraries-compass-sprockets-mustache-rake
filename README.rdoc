Tool for integrating Compass, Sprockets, and Mustache into NYU Libraries projects.

Steps:
  $ git clone git://github.com/scotdalton/nyu-libraries-compass-sprockets-mustache-rake.git
  $ bundle install
  $ rake nyu_libraries_assets:compile
  
Assumes the following directory name:
* sass
* stylesheets
* images
* javascripts