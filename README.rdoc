Sample integration of Compass, Sprockets, and Mustache.

Steps:
  $ git clone git://github.com/scotdalton/sample-compass-sprockets-mustache-rake.git
  $ bundle install
  $ rake nyu_assets:compile
  
Assumes the following directory name:
* sass
* stylesheets
* images
* javascripts